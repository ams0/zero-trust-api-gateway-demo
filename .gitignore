# Secrets — never commit a real Traefik Hub token or any rendered secret
.env
.env.*
*.token
secret*.yaml
*-secret.yaml
license.yaml

# Kubeconfigs / cluster credentials
kubeconfig*
*.kubeconfig
kube.conf

# Local build / runtime artifacts
*.tar
*.tgz
# NOTE: apps/echo-svc/go.sum IS committed (reproducible, pinned, vuln-free build)

# kubectl/debug dumps
temp.json
*.dump.json
*.log

# Helm
charts/
*.tgz

# OS / editor cruft
.DS_Store
Thumbs.db
*.swp
*~
.idea/
.vscode/
